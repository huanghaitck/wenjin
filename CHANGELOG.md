# Changelog

## 0.1.4-rc2 - 2026-09-03

- 领域 Agent 工具面收敛：模型可见工具从 24 个收敛到 6 个门面入口（灾害表格处理、逐页处理全书、登记书籍列表、历史—现代政区查询、结果检查器、朝代纪年词表），减少模型路由错误；
- 新增研究线程归档与批量倒回：对话标题栏"归档此对话"一键归档；设置 → 记忆与数据 → 已归档对话可恢复或彻底删除；领域 Agent 线程同样支持归档/删除；
- 左侧研究线程列表右键菜单支持重命名/归档/彻底删除，不再逐行铺开操作按钮；
- 新增灾害批量引擎：SQLite 中间层（rows/issues/operations/audit/versions 五张表）+ 结构化批量规则（等于/包含/多值/空非空/年份范围/复核原因/事件组）+ 批量倒回双轨（按操作撤销/按版本水位线回退）+ 字段权限（坐标/今地名/星号/等级四类程序计算字段禁止直接设值）；
- 大表流式导出：openpyxl write_only 模式，38,520 行导出从 731 秒降至 21 秒（35 倍），逐格值与普通模式一致；
- 修复领域 Agent "新建线程"和"重命名线程"按钮在桌面壳内无响应的问题（根因：Tauri WebView2 不支持 window.prompt，改用项目已有 askText 对话框）；
- 修复删除线程时因外键约束导致的 FOREIGN KEY constraint failed（改为动态扫描所有引用表级联清理）；
- 来源格式校验放宽：依《历史研究》注释体例增加非志书分支（纂修者前缀、版本尾注、括注、报刊日期），597 条格式复核项收敛至 41 行真歧义；
- `computer.file_search` 扫描上限从 50,000 放宽至 2,000,000；
- 工具外显中文名称（如"检查半成品表（inspect_half_finished_workbook）"），方便用户理解模型正在调用什么。

## 0.1.4 - 2026-09-01

- 主对话和领域 Agent 共用阅读位置、“回到最新消息”和运行状态条；运行开始后输入框立即转为方向调整，停止后只能由新的用户指令继续；
- 新增复核工作簿往返闭环：标准转换每轮默认交付待人工复核 XLSX；对话决定可写入新的复核副本，再生成复核合并版主表、下一轮待复核表和审计表，不覆盖原文件；
- 已安装领域 Agent 的动态工具契约变化时自动切换底层 Codex 线程，避免旧线程继续使用过期工具清单；
- “AI 与 Agent”收拢为“设置”，统一模型、Agent 路由、人格、记忆与数据、连接、插件、外观和系统页；新增内置帮助入口；
- 新增只读系统自检与可回滚的安全修复。修复只处理有可靠本地来源记录的损坏插件，不修改史料、规则或密钥；
- 完成“墨线编辑部”界面重构：减少堆叠卡片，左右侧栏均可调宽，右栏关闭后仍可从顶部状态带恢复；技能库改为可点开详情的两列清单，新建项目重新显示问津工作区/本地文件夹选择；
- 辅助模型岗位默认继承主模型，单击或右键按岗位单独设置，不再同时铺开所有完整表单；
- 更新内置史学语言返修技能，增加年表式综述段与返修分数反弹的识别，同时保留证据、页码和人工决定边界；
- 新增主程序 6 项 MCP 契约检查、关键 Playwright 交互回归、领域 Agent 23 项工具契约检查、构建身份校验与离线包清单验证。

平台状态：Windows 安装包与完整离线包已经发布；Apple Silicon `.app` 与 `.dmg` 已在 GitHub macOS runner 构建并完成 sidecar 启动测试。macOS 预览包仅使用 ad-hoc 签名，尚未完成 Developer ID 签名和 Apple 公证。

已知限制：在没有正式签名密钥和发布端点前，不启用静默在线自动更新；发布继续使用经校验的完整安装包和离线包。

## 0.1.3 - 2026-08-29

- 主 Agent 正式以内置 Codex app-server 作为单一工具循环，保留问津项目、来源、证据、审批和写作状态；
- 修复共享图书馆路径未进入主 Agent 运行上下文的问题，网页、CLI和自然对话现在访问同一座研究图书馆；
- 图书馆检索支持多关键词组合，主 Agent会把模糊研究描述拆成短关键词、检查候选并在无结果时改用近义词；
- 新增受权限控制的`library.add_to_project`，可在自然对话中读取精确版本、请求批准并自动生成PDF页级文本或DOCX定位文本；
- 图书馆入库先整理题名页、首页书目信息和分诊预览，书目信息仍可人工修正；完整页级清洗在采用到项目时执行；
- 作品关系图默认排除读书笔记；用户明确要求时可通过Agent/API临时纳入，书目实体与项目内容作为同一画布中的独立视图；
- 作品关系、书目实体和项目内容分别使用COSE、fCoSE和层级布局，消除网格化、中心重叠和等待布局时的错误初始画面；
- 单击节点聚焦邻接关系，双击才打开统一书目/来源详情；已采用作品可直接打开对应项目原页；
- 图谱页移除重复作品卡、Markdown卡和关系长清单，侧栏按需展开，图标按钮与中英文提示同步；
- 实际自然语言验收完成“模糊找书—读取版本—等待批准—采用并清洗—同线程继续”；完整回归420项通过。

## 0.1.3 RC1 - 2026-08-27

- 主 Agent 新增只读运行环境自检，可识别应用内置运行时、uv 管理的 Python 与 `pwsh`；不再把 WindowsApps 占位程序或 `powershell1` 当作可执行环境；
- 新增受审批的可选运行时修复工具；自包含领域 Agent 优先使用自己的 MCP 程序，不再先搜索工作流脚本或批量导入 Python 包；
- 模型调用超时改为按推理模式和本轮工具结果动态上调，外部命令继续由任务显式设置，避免取消所有超时后形成失控进程；
- 书目与实体图谱启用 fCoSE 原生分量打包，删除会把多个分量重新堆到中心的手工位移；
- 自动盘点跳过 Bench、测试缓存和明显临时工程文件；清理工具只移除书目登记，不删除原文件，并先备份图书馆数据库；
- 记录 OpenAI Codex app-server 的线程、流式事件、审批、中断与恢复边界；问津复用这些生命周期原则，不复制协议、不绕过研究证据门禁。

## 0.1.2 - 2026-08-26

图书馆与书目关系图谱更新。

- 入库题名改为清洗后的文件名优先；纯编号或哈希文件名才回退到文档内嵌题名；
- 清除把CNKI Reader、Acrobat、FreePic2Pdf、Administrator等制作软件误认成作者、出版社或出版年的情况；
- 相同题名且可判定为同一作品的文件自动并入同一作品，保留多个文件位置与精确版本，不移动或删除原件；
- 增加“读书笔记”独立书架；读书报告、阅读札记和课程阅读作业不进入书目关系图谱；
- 书目关系图谱改为仅显示作品节点，不再外显作者、年份、书架和材料类型等数据库中间节点；
- 作品节点按原始史料、期刊论文、学术专著、个人论文与稿件、工具书及待分类着色；
- 关系线限定为同作者、同出版社、同期刊、同年代（十年），以及引用、材料使用、评述、翻译和提及；知识关系使用有向箭头；
- 带页码Markdown中的脚注、尾注和参考文献精确题名自动建立引用关系，正文精确题名自动建立提及关系；研究者可排除误匹配；
- 引入MIT许可的Cytoscape fCoSE力导向布局：有关系的作品自然聚集，孤立作品稳定散布外围；
- 图谱支持滚轮缩放、空白处或中键平移、悬停题名、单击邻接聚焦、双击书目卡与搜索子图；
- 修复大量节点时网格化、竖线化、无效坐标、题名遮挡和系统标签造成的“毛线团”问题；
- 完整盘点与去重流程现在可明确排除期刊拆分页、检测报告和测试产物。

升级不会移动或修改原始文献。首次打开0.1.2时，建议在图书馆重新执行一次只读盘点，再人工确认新增或变化文件。

## 0.1.1 - 2026-08-26

首个可安装的公开预览版。

- 增加默认“项目工作区”：可在指定本地文件夹新建项目、登记已有项目，并集中查看阶段、研究对象、待决定事项和最近活动；
- 自动备份覆盖工作区登记的外置项目，并保存数据库及项目材料副本，恢复时建立完整的新项目副本；
- 增加本地项目自动备份、完整性检查和非覆盖恢复；
- 增加史学记忆与工程记忆的人工批准提升接口；
- 增加主模型、辅助角色、MoA、研究人格、MCP与领域 Agent 配置；DeepSeek、Ollama和OpenAI兼容接口可自动读取模型列表，推理模式与强度按实际模型能力显示；
- 增加中英文核心界面；
- 增加多个可命名史料长编、连续阅读Markdown、数据图表、地图、图片与两层知识图谱；图谱支持邻接高亮、双击查看书目或返回来源页；
- 增加独立领域 Agent 工作区、引导式创建、文件与图片上传、工具过程和可打开产物；每个领域 Agent 按“包名＋模型岗位”隔离保存领域推理、主视觉、视觉复核和备用评审配置；
- 增加中立领域 Agent 工程、受控创建与校验、本地ZIP/目录安装、用户数据库绑定和自包含MCP运行时；
- 内置16项史学研究Skill Pack及最新证据保真中文史学语言返修Skill，不再依赖开发机插件缓存；
- 增加Codex双向桥接；
- 内置`agent-browser 0.33.0` Windows运行时；完整离线ZIP附WebView2独立安装程序，Agent可读取可见会话并在批准域名内导航，系统Edge/Chrome承担可见研究浏览器；
- 增加“请求批准、帮我批准、完全访问”三档全电脑Agent权限与自动审批审计回执；
- 增加运行中方向调整、安全停止、队列消息编辑/删除、对话分支、复制和引用；人工批准工具后会在同一Run自动续跑；
- 主 Agent 恢复本地文件检索、程序与命令、办公文件、受控浏览器、联网检索和Skill创建等通用能力；上下文达到模型窗口约90%时自动压缩；
- 增加问津自有普通微信扫码私聊网关，直接连接腾讯iLink，不依赖Hermes或OpenClaw运行时；
- 图书馆盘点改为后台可恢复任务，并支持建议分类后批量入库；
- 修复桌面启动、侧车清理、打开图片/产物/网址、可调三栏布局、写作门禁、引用输出、阅读来源身份、大目录盘点、空图谱布局和审批后续跑问题。

验收：问津392项测试与12项子测试通过；Windows安装版已完成真实模型、人工审批、受控网页读取、领域 Agent 模型隔离、图谱、史料长编和全页布局验收。

已知限制：安装包未签名，无自动更新；macOS尚未真机构建；复杂DOCX往返仍需在Word中人工复核。

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "The bundled desktop shell may call only application commands exposed by Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
